# 保存为 function-hello.yaml
apiVersion: faas.example.com/v1alpha1
kind: Function
metadata:
  name: hello
  namespace: default
spec:
  code: |
    def main():
      return "hello from runner"
  runtime: python3.9
  minReplicas: 1
  maxReplicas: 3